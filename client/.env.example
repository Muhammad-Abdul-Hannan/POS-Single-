# Leave unset for local dev — Vite's dev-server proxy (see vite.config.js)
# forwards /api to the local backend automatically.
#
# In production (Vercel), set this to your deployed backend's full API URL,
# e.g. https://your-api.onrender.com/api
VITE_API_URL=
